# Command Reference

> **This appendix is planned for a future release.** It will provide a comprehensive reference of all commands, subcommands, flags, and environment variables.
>
> In the meantime, use `lc --help` and `lc <command> --help` for flag reference.
