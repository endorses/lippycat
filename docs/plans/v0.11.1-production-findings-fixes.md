# v0.11.1 Production Findings Fixes

- [x] Remove redundant local-capture packet copying and decoding while preserving packet ownership and metadata.
- [x] Decouple live capture normalization/bridge processing enough to avoid unnecessary capture-path stalls.
- [x] Collect libpcap receive/drop statistics and surface them in local TUI health statistics and capture logs.
- [x] Ensure application `PacketBuffer` drops are accurately surfaced in the local TUI rather than estimated solely from bridge batches.
- [x] Add regression tests for packet reuse, capture drop propagation, and bridge behavior.
- [x] Make TCP auto-tuner state transitions idempotent and avoid repeated backpressure logging.
- [x] Separate the TCP auto-tuner pressure signal from the soft monitoring threshold, with tests for sustained legitimate stream counts.
- [x] Format all changed files and run targeted and broad relevant tests.
- [x] Commit the completed code and plan updates.
