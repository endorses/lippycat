# Test certificates are committed to git for convenience
# This allows immediate test execution without a setup step
# Security scanners are configured to ignore this directory (see .gitguardian.yaml)

# Only ignore intermediate certificate generation files (not needed in git)
*.csr
*.srl
*.cnf
