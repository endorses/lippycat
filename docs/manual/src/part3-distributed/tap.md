# Standalone Mode with `lc tap`

> **This chapter is planned for a future release.** It will cover:
>
> - When to use `lc tap` vs `lc hunt` + `lc process`
> - Tap as "hunt + process − gRPC"
> - Configuration and usage
> - Upstream forwarding
>
> For current documentation, see `cmd/tap/README.md` in the repository.
