# CLI Administration

> **This chapter is planned for a future release.** It will cover:
>
> - `lc show` commands: status, hunters, topology, filter, config
> - `lc list` commands: interfaces, filters
> - Filter lifecycle management
>
> For current documentation, see `cmd/show/README.md` and `cmd/list/README.md` in the repository.
