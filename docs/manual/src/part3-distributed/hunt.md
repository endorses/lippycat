# Edge Capture with `lc hunt`

> **This chapter is planned for a future release.** It will cover:
>
> - Transitioning from `lc sniff` to `lc hunt`
> - Connecting to a processor
> - Protocol-specific hunters (VoIP, DNS)
> - Resilience: flow control, disk overflow buffer, circuit breaker, reconnection
> - Performance tuning: batch sizing, GPU acceleration, BPF optimization
>
> For current documentation, see `cmd/hunt/README.md` in the repository.
